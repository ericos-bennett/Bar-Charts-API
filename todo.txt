Different colors for each bar (as an array not a string in options.)
Work on stacked bars

Y-axis labels do not work well with very small decimal numbers
If x-axis value labels are exceptionally long, they can overflow out of the element box
